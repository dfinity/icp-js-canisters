{
  "name": "@dfinity/icp-js-canisters-tests-integration",
  "private": true,
  "type": "module",
  "scripts": {
    "pretest": "bash ../../scripts/prepare-integration-tests",
    "test": "npm run pretest && vitest && npm run posttest",
    "posttest": "bash ../../scripts/finish-integration-tests"
  }
}
